spring:
  application:
    name: ui-gateway
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  file: test.log